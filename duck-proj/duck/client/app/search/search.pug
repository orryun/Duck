#search
    div.panel.panel-default#search-panel
        div.panel-heading 
            div.panel-title {{ title }}
        div.panel-body
            ul.list-group
                li.list-group-item
                    label.control-label Title
                        input(ng-model='title')
                    span  (Hint: Change me!)
                
                li.list-group-item
                    label.control-label Term
                        input(ng-model='searchTerm', ng-init="searchTerm", nino-enter="search(searchTerm)")
                    span  (Hint: Enter on me!)
                
                li.list-group-item(ng-repeat="result in searchResults")
                    strong Name:
                    span  {{ result.name }}
                    br
                    strong Data:
                    div(ng-bind-html="result.data")
                    
                li.list-group-item
                    ui-select(ng-model="selected.target", theme="bootstrap")
                        ui-select-match(placeholder="Select a target") {{ $select.selected.name }}
                        ui-select-choices(repeat="target in targets")
                            div(ng-bind-html="target.name | highlight: $select.search")
